"""
Django settings for epcdata project.

Generated by 'django-admin startproject' using Django 3.2.6.

For more infor# Define custom apps that should replace Oscar's defaults
CUSTOM_OSCAR_APPS = [
    'checkout',  # Our custom checkout app
    'shipping',  # Our custom shipping app with weight-based methods
    # 'shipping_dashboard',  # Disabled - conflicts with our custom shipping models
] on this file, see
https://docs.djangoproject.com/en/3.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/3.2/ref/settings/
"""

from pathlib import Path
import os
from dotenv import load_dotenv

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent

# Determine which environment file to load
# Check for production environment indicator based on switch_env.ps1 logic or server IP
is_production = False

# Function to detect if running on production server
def detect_production_server():
    try:
        import socket
        # Get the server's IP address
        hostname = socket.gethostname()
        local_ip = socket.gethostbyname(hostname)
        
        # Check if running on the production VPS IP
        production_ips = ['80.95.207.42', '80.95.207.45']  # External IPs only (removed 127.0.1.1 as it's too generic)
        
        # Also check for environment variables that indicate VPS deployment
        vps_indicators = [
            os.environ.get('SERVER_IP') == '80.95.207.42',
            os.environ.get('HOSTNAME', '').lower().find('vps') != -1,
            os.environ.get('HOSTNAME', '').lower().find('server') != -1,
            hostname.lower() == 'rentals',  # VPS hostname
        ]
        
        print(f"🔍 Debug: hostname={hostname}, local_ip={local_ip}")
        print(f"🔍 Debug: SERVER_IP={os.environ.get('SERVER_IP')}")
        print(f"🔍 Debug: HOSTNAME={os.environ.get('HOSTNAME')}")
        
        return local_ip in production_ips or any(vps_indicators)
    except Exception as e:
        print(f"🔍 Debug: Error in detect_production_server: {e}")
        return False

# Debug: Show what files exist
print(f"🔍 Debug: .env exists = {os.path.exists(BASE_DIR / '.env')}")
print(f"🔍 Debug: .env.production exists = {os.path.exists(BASE_DIR / '.env.production')}")
print(f"🔍 Debug: .prod exists = {os.path.exists(BASE_DIR / '.prod')}")
print(f"🔍 Debug: DJANGO_ENV = {os.environ.get('DJANGO_ENV', 'NOT SET')}")

# Check if explicitly set to production via environment variable
if os.environ.get('DJANGO_ENV') == 'production':
    is_production = True
    print("🌐 Production mode: DJANGO_ENV=production")
# Check if running on production server IP
elif detect_production_server():
    is_production = True
    print("🌐 Production mode: Detected VPS server IP (80.95.207.42)")
# Check if .prod file exists (created by switch_env.ps1 for production)
elif os.path.exists(BASE_DIR / '.prod'):
    is_production = True
    print("🌐 Production mode: .prod file exists")
# DO NOT automatically switch to production just because .env.production exists
# This allows both files to coexist without affecting local development

if is_production:
    # Try to load production environment variables
    if os.path.exists(BASE_DIR / '.prod'):
        load_dotenv(BASE_DIR / '.prod', override=True)
        print("🌐 Loading PRODUCTION environment from .prod")
    elif os.path.exists(BASE_DIR / '.env.production'):
        load_dotenv(BASE_DIR / '.env.production', override=True)
        print("🌐 Loading PRODUCTION environment from .env.production")
    print(f"DEBUG: Production mode - ALLOWED_HOSTS env var = {os.getenv('ALLOWED_HOSTS', 'NOT SET')}")
else:
    # Load local development environment variables
    if os.path.exists(BASE_DIR / '.env'):
        load_dotenv(BASE_DIR / '.env', override=True)
        print("🏠 Loading LOCAL environment from .env")
    else:
        print("⚠️ No .env file found, using default settings")
    print(f"DEBUG: Local mode - ALLOWED_HOSTS env var = {os.getenv('ALLOWED_HOSTS', 'NOT SET')}")

# Final debug summary
print(f"🎯 FINAL RESULT: {'PRODUCTION' if is_production else 'LOCAL DEVELOPMENT'} mode selected")
print("=" * 60)


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/3.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = os.getenv('SECRET_KEY', 'django-insecure-aiqpmaxs^h@-@r#-nvtu)%p73-0#h3jkb_q99zj6+znx%+#s@6')

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = os.getenv('DEBUG', 'True').lower() == 'true'

# Allow hosts from environment variable or use defaults
ALLOWED_HOSTS_STR = os.getenv('ALLOWED_HOSTS', '80.95.207.42,80.95.207.45,vanparts-direct.co.uk,www.vanparts-direct.co.uk,maxusparts.co.uk,www.maxusparts.co.uk,localhost,127.0.0.1,[::1]')
ALLOWED_HOSTS = [host.strip() for host in ALLOWED_HOSTS_STR.split(',')]
print(f"DEBUG: ALLOWED_HOSTS = {ALLOWED_HOSTS}")

# Security settings for production
SECURE_CROSS_ORIGIN_OPENER_POLICY = None  # Fix the COOP header warning
SECURE_BROWSER_XSS_FILTER = True
SECURE_CONTENT_TYPE_NOSNIFF = True
X_FRAME_OPTIONS = 'DENY'

# CSRF settings for HTTPS
CSRF_TRUSTED_ORIGINS = [
    'https://vanparts-direct.co.uk',
    'https://www.vanparts-direct.co.uk',
    'https://maxusparts.co.uk',
    'https://www.maxusparts.co.uk',
]

# Additional CSRF settings for reverse proxy setups
CSRF_USE_SESSIONS = False
CSRF_COOKIE_HTTPONLY = False  # Allow JavaScript access if needed
CSRF_COOKIE_SAMESITE = 'Lax'  # Less restrictive for cross-origin issues


# Application definition
import oscar

# Define custom apps that should replace Oscar's defaults
CUSTOM_OSCAR_APPS = [
    'checkout',  # Our custom checkout app
    'shipping',  # Our custom shipping app with weight-based methods
    # 'shipping_dashboard',  # Our custom shipping dashboard app - disabled due to model conflicts
]

# Get Oscar's apps and replace checkout with our custom one
OSCAR_APPS = []
for app in oscar.INSTALLED_APPS:
    if 'checkout' in app and 'checkout' not in OSCAR_APPS:
        # Skip Oscar's checkout app, we'll use our custom one
        continue
    if 'shipping' in app and 'shipping' not in OSCAR_APPS:
        # Skip Oscar's shipping app, we'll use our custom one
        continue
    if 'shipping_dashboard' in app and 'shipping_dashboard' not in OSCAR_APPS:
        # Skip Oscar's shipping dashboard app, we'll use our custom one
        continue
    OSCAR_APPS.append(app)

INSTALLED_APPS = [
    # Your apps first (so they can override Oscar apps)
    'epcdata.apps.EpcdataConfig',  # Updated to use AppConfig for signal loading
    'motorpartsdata',
    'payment',   # Custom payment app for Worldpay
    'checkout',  # Custom checkout app (must be before Oscar apps)
    'phone_patch.apps.PhoneNumberPatchConfig',  # Monkey patch for phone validation
    'shipping.apps.ShippingConfig',  # Custom shipping app with weight-based methods
    'shipping_dashboard.apps.ShippingDashboardConfig',  # Minimal shipping dashboard to satisfy Oscar dependencies
    'rest_framework',
    'sorl.thumbnail',  # Required for Oscar image thumbnails
    'django_countries',  # For country selection in shipping
] + OSCAR_APPS

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'whitenoise.middleware.WhiteNoiseMiddleware',  # Add WhiteNoise for static files
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
    
    # Oscar middleware
    'oscar.apps.basket.middleware.BasketMiddleware',
]

# Debugging: Enable debug for static files in production temporarily
if DEBUG:
    WHITENOISE_AUTOREFRESH = True
    WHITENOISE_USE_FINDERS = True
    # Add debug info for static files
    import logging
    logging.getLogger('whitenoise').setLevel(logging.DEBUG)
    logging.getLogger('django.contrib.staticfiles').setLevel(logging.DEBUG)

ROOT_URLCONF = 'epcdata.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [os.path.join(BASE_DIR, 'templates')],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
                
                # Oscar context processors (Oscar 3.2 compatible)
                'oscar.apps.search.context_processors.search_form',
                'oscar.apps.checkout.context_processors.checkout',
                'oscar.core.context_processors.metadata',
                
                # Custom context processors
                'epcdata.context_processors.uren_context',
                'epcdata.context_processors.vehicle_brands_context',
            ],
        },
    },
]

WSGI_APPLICATION = 'epcdata.wsgi.application'


# Database
# https://docs.djangoproject.com/en/3.2/ref/settings/#databases

# Database configuration - all values come from environment files
DATABASES = {
    'default': {
        'ENGINE': os.getenv('DB_ENGINE', 'django.db.backends.postgresql'),
        'NAME': os.getenv('DB_NAME'),
        'USER': os.getenv('DB_USER'),
        'PASSWORD': os.getenv('DB_PASSWORD'),
        'HOST': os.getenv('DB_HOST', 'localhost'),
        'PORT': os.getenv('DB_PORT', '5432'),
    }
}

# Validate that required database environment variables are set
if not DATABASES['default']['NAME']:
    raise ValueError("DB_NAME environment variable is required")
if not DATABASES['default']['USER']:
    raise ValueError("DB_USER environment variable is required")
if not DATABASES['default']['PASSWORD']:
    raise ValueError("DB_PASSWORD environment variable is required")

print(f"🗄️ Database config: {DATABASES['default']['NAME']} as {DATABASES['default']['USER']} on {DATABASES['default']['HOST']}")


# Password validation
# https://docs.djangoproject.com/en/3.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/3.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/3.2/howto/static-files/

STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

# Static files directories - collect from these locations
STATICFILES_DIRS = [
    os.path.join(BASE_DIR, 'motortemplate'),  # Direct mapping - files available as /static/uren/assets/...
]

# Static files finders
STATICFILES_FINDERS = [
    'django.contrib.staticfiles.finders.FileSystemFinder',
    'django.contrib.staticfiles.finders.AppDirectoriesFinder',
]

# WhiteNoise configuration (primarily for production)
if not DEBUG:
    # Production static file serving with WhiteNoise
    # Use StaticFilesStorage instead of CompressedManifestStaticFilesStorage to avoid missing .map file issues
    STATICFILES_STORAGE = 'whitenoise.storage.CompressedStaticFilesStorage'
    WHITENOISE_USE_FINDERS = False
else:
    # Development static file serving
    STATICFILES_STORAGE = 'django.contrib.staticfiles.storage.StaticFilesStorage'
    WHITENOISE_USE_FINDERS = True  # Allow WhiteNoise to serve from STATICFILES_DIRS directly
    WHITENOISE_AUTOREFRESH = True  # Auto-refresh files in development

WHITENOISE_SKIP_COMPRESS_EXTENSIONS = ['js', 'css', 'map']  # Skip compression for debugging and source maps

# Additional WhiteNoise settings for better static file serving
WHITENOISE_INDEX_FILE = True  # Serve index files
WHITENOISE_MIMETYPES = {
    '.css': 'text/css',
    '.js': 'application/javascript',
    '.map': 'application/json',  # Handle source map files
}

# WhiteNoise settings to handle missing source map files gracefully
WHITENOISE_MAX_AGE = 31536000  # 1 year cache
WHITENOISE_SKIP_COMPRESS_EXTENSIONS.extend(['woff', 'woff2', 'ttf', 'eot', 'svg'])  # Skip font compression

# Media files
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# Sites framework
SITE_ID = 1

# Oscar configuration
from oscar.defaults import *

# Oscar settings
OSCAR_DEFAULT_CURRENCY = 'GBP'
OSCAR_SHOP_NAME = 'EPC Motor Parts Store'
OSCAR_SHOP_TAGLINE = 'Your trusted motor parts supplier'

# Disable email confirmation for user registration in development
OSCAR_SEND_REGISTRATION_EMAIL = False

# Enable order confirmation emails
OSCAR_SEND_ORDER_EMAILS = True
OSCAR_EAGER_LOADED_ORDER_STATUSES = ['Being processed', 'Shipped', 'Complete']

# Configure Oscar URLs for "View on site" functionality
OSCAR_HOMEPAGE = '/'

# Haystack (for Oscar search)
HAYSTACK_CONNECTIONS = {
    'default': {
        'ENGINE': 'haystack.backends.simple_backend.SimpleEngine',
    },
}

# Default primary key field type
# https://docs.djangoproject.com/en/3.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Logging configuration for debugging
LOGGING = {
    'version': 1,
    'disable_existing_loggers': False,
    'handlers': {
        'console': {
            'class': 'logging.StreamHandler',
        },
    },
    'loggers': {
        'django': {
            'handlers': ['console'],
            'level': 'INFO',
        },
        'oscar': {
            'handlers': ['console'],
            'level': 'INFO',
        },
        'django.contrib.staticfiles': {
            'handlers': ['console'],
            'level': 'DEBUG',
        },
        'whitenoise': {
            'handlers': ['console'],
            'level': 'DEBUG',
        },
    },
}

# Authentication settings
LOGIN_URL = '/accounts/login/'
LOGIN_REDIRECT_URL = '/accounts/'
LOGOUT_REDIRECT_URL = '/'

# Authentication backends
AUTHENTICATION_BACKENDS = [
    'oscar.apps.customer.auth_backends.EmailBackend',
    'django.contrib.auth.backends.ModelBackend',
]

# Email configuration for development
# EMAIL_BACKEND = 'django.core.mail.backends.console.EmailBackend'  # Disabled to use Gmail SMTP

# Alternatively, to disable emails completely in development:
# EMAIL_BACKEND = 'django.core.mail.backends.dummy.EmailBackend'

# Django Countries Configuration
COUNTRIES_FIRST = ['GB', 'US', 'IE', 'FR', 'DE']  # Prioritize common shipping countries
COUNTRIES_FIRST_REPEAT = True  # Show priority countries at top and in alphabetical order
COUNTRIES_FIRST_BREAK = '────────────'  # Visual separator in dropdown

# Worldpay Gateway API Configuration (Direct Payment Processing)
# Based on successful testing - processed £20 payment with HTTP 201 response
# Working API endpoint: https://try.access.worldpay.com/payments/authorizations
# Content-Type: application/vnd.worldpay.payments-v6+json
# Entity ID: PO4080334630 (proven working)

WORLDPAY_GATEWAY_URL = os.getenv('WORLDPAY_GATEWAY_URL', 'https://try.access.worldpay.com/payments/authorizations')
WORLDPAY_USERNAME = os.getenv('WORLDPAY_USERNAME', 'evQNpTg2ScurKUxK')  # Working test username
WORLDPAY_PASSWORD = os.getenv('WORLDPAY_PASSWORD', 'evQNpTg2ScurKUxK')  # Working test password  
WORLDPAY_ENTITY_ID = os.getenv('WORLDPAY_ENTITY_ID', 'PO4080334630')  # Working entity ID

# Test mode setting (use test endpoints for development)
WORLDPAY_TEST_MODE = os.getenv('WORLDPAY_TEST_MODE', 'True').lower() == 'true'

# Legacy hosted payments settings (for backwards compatibility)
WORLDPAY_API_URL = os.getenv('WORLDPAY_API_URL', 'https://try.access.worldpay.com/sessions')

# Override URLs for test mode (Gateway API uses different endpoints)
if WORLDPAY_TEST_MODE:
    WORLDPAY_GATEWAY_URL = 'https://try.access.worldpay.com/payments/authorizations'
else:
    WORLDPAY_GATEWAY_URL = 'https://access.worldpay.com/payments/authorizations'

# Email Configuration - Gmail SMTP for Google Workspace
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = os.getenv('EMAIL_HOST_USER', 'sales@vanpartsdirect4u.co.uk')
EMAIL_HOST_PASSWORD = os.getenv('EMAIL_HOST_PASSWORD', 'xivu xwyk ebef avum')  # Gmail App Password
DEFAULT_FROM_EMAIL = os.getenv('DEFAULT_FROM_EMAIL', 'sales@vanpartsdirect4u.co.uk')
SERVER_EMAIL = os.getenv('SERVER_EMAIL', 'sales@vanpartsdirect4u.co.uk')

# Email settings for Oscar e-commerce
OSCAR_FROM_EMAIL = DEFAULT_FROM_EMAIL
OSCAR_SEND_REGISTRATION_EMAIL = True

# Shipping Configuration - Custom weight-based shipping
# Shipping configuration is now handled by our custom shipping app
OSCAR_SHIPPING_DEFAULT_WEIGHT = 0  # Default weight for products without weight attribute

# Country Configuration - UK only shipping
OSCAR_DEFAULT_COUNTRY_CODE = 'GB'  # United Kingdom as default
OSCAR_SHOP_COUNTRIES = ['GB']  # Only allow UK

# Django Countries Configuration
COUNTRIES_ONLY = ['GB']  # Restrict django-countries to UK only
COUNTRIES_OVERRIDE = {
    'GB': 'United Kingdom',
}

# UK VAT Configuration (20%)
# Enable tax calculation for UK VAT
OSCAR_DEFAULT_TAX_RATE = 0.20  # 20% UK VAT
OSCAR_USE_TZ = True  # Use timezone-aware calculations

# Tax settings for UK VAT
OSCAR_PRICES_INCL_TAX = True  # Display prices including VAT by default
OSCAR_SHOP_NAME = "EPC Parts Store"
OSCAR_CURRENCY_SYMBOL = "£"

# VAT calculation settings
import decimal
UK_VAT_RATE = decimal.Decimal('0.20')  # 20% VAT
